module loom.cortex.action.consistency {
	exports io.metaloom.loom.cortex.action.consistency;

	requires utils;
	requires hash_utils;
	requires loom.client.grpc;
	requires loom.proto;
	requires loom.cortex.api;
	requires loom.cortex.common;
	requires java.base;
}