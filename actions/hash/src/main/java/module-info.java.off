module loom.worker.action.hash {
	exports io.metaloom.worker.action.hash;

	requires utils;
	requires hash_utils;
	requires loom.client.common;
	requires loom.client.grpc;
	requires loom.proto;
	requires loom.worker.api;
	requires loom.worker.common;
}