package io.metaloom.cortex.api.media.type;

/**
 * List of {@link LoomMetaType}'s which have core support.
 */
public enum LoomMetaCoreType implements LoomMetaType {

	XATTR,

	FS,

	HEAP, 
	
	AVRO;
}
